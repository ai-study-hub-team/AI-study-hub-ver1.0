server:
  port: 8080

spring:
  application:
    name: backend

  datasource:
    url: jdbc:mysql://localhost:3306/ai_study_hub_test?useSSL=false&serverTimezone=Asia/Ho_Chi_Minh&allowPublicKeyRetrieval=true
    username: root
    password: 123456

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true